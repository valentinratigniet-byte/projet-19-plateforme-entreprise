name: "projet19_plateforme_entreprise"
version: "1.0.0"
config-version: 2

profile: "projet19"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  projet19_plateforme_entreprise:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
